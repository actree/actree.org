<div class="banner">
    <div class="container">Hier findest du Links zu fairen und ökologischen Produkten, kostenlosen Bildungsangeboten, Ideen und Informationen zu nachhaltigem Konsum, und vielem mehr.</div>
</div>
<section class="browse container page-container">
    <div class="pure-g">
        <ul class="sidemenu pure-u-1-4">
            <li><a href="/browse">NEUESTE</a></li>
            {{#each data.tags}}<li>
                <a href="/browse/{{this}}">{{name}}</a>
            </li>{{/each}}
        </ul>
        <div class="pure-u-3-4">
            {{# each data.entries.results}}
                <!-- <div data-ks-editable="{{#if ../user}}{{adminEditableUrl ../../user _id}}{{/if}}" class="post"> -->
                <div class="entry-small pure-g">
                  <img class="entry-img pure-u-1-6" src="{{ oldImage }}">
                  <div class="entry-content pure-u-5-6">
                    <a href="{{ entryUrl slug }}" class=""><h4 class="title-major">
                        {{ title }}
                    </h4>
                    </a>
                    <div class="entry-meta ">
                      <ul>{{#each tags}}
                        <li>{{>tag}}</li>
                      {{/each}}
                      </ul>
                    </div>
                  </div>

                </div>
            {{/each}}
        </div>
    </div>

    </div>
    <!-- {{>pagination}} -->
</section>
